import React from "react";

const Header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Click the show characters</h1>
    <h3 className="text-center">Click on an image to get points, but don't click on any more than once or the game will end</h3>
  </div>
)
export default Header;